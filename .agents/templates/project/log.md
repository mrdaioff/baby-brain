---
type: project-log
project: {slug}
month: {YYYY-MM}
---

# {Project Name} — log {YYYY-MM}

> One file per month at `projects/<slug>/log/YYYY-MM.md`. Append-only: add a
> new section at the end, never rewrite an earlier one. This is where dated
> narrative lives — build-log entries, call summaries, post-mortems, the
> reasoning behind a decision. `context.md` gets the one-line outcome; this
> file gets the story.
>
> The project router appends a section here for every matched call, and
> creates the month's file if it does not exist yet.
>
> Delete this block once the first real entry lands.

## {YYYY-MM-DD} — {title}

{What happened, what changed, decisions with their reasoning, anything worth
keeping. A few lines to a few paragraphs.}

Artifact: [[brand/artifacts/{YYYY-MM-DD_HHmmss_Title}]]
