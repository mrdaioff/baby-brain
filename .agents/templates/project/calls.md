---
project: {slug}
type: call-log
updated: {YYYY-MM-DD}
---

# {Project Name} — Calls

> One row per call. The project router appends here automatically when it
> matches an ingested transcript to this project, using exactly the column
> order below — keep it.
>
> Links are relative from `projects/{slug}/`, hence the `../../` prefix.
>
> **Seed this file manually when you create the project.** If calls with this
> client already exist in `brand/artifacts/`, they belong here from day one. A
> call log that starts empty while months of real history sits in the repo
> reads as authoritative and is not.
>
> Delete this block once the project is real.

| Date | Title | Artifact | Transcript |
|---|---|---|---|
